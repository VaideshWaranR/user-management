import React from 'react'

import Envelope from './Envelope'

const Main = () => {
  return (
    <div className='flex items-center h-[100vh] sm:w-[75%] md:w-[80%] sm:fixed sm:left-[30%] md:left-[20%] '>
      <Envelope/>
    </div>
  )
}

export default Main